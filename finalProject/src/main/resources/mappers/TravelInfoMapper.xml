<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.dao.TravelInfoDAO">

	<!-- travel_info 테이블에 일정 넣기 -->
    <insert id="insertTravelInfo">
	    INSERT INTO travel_info (info_id, member_email, info_name, trip_place, start_date, end_date, who_tag, style_tag)
	    VALUES (info_id_seq.NEXTVAL, #{member_email}, #{info_name}, #{trip_place}, #{start_date}, #{end_date}, #{who_tag}, #{style_tag})
	</insert>
	
	<!-- info_id 가져오기 -->
	<select id='selectTravelInfo' resultType='string'>
		SELECT info_id_seq.currval FROM DUAL
	</select>
	
	<select id="existsByMemberEmail" resultType="int">
       SELECT CASE WHEN COUNT(*) > 0 THEN 1 ELSE 0 END
       FROM TRAVEL_INFO
       WHERE MEMBER_EMAIL = #{memberEmail}
   	</select>
   	
    <!-- 모든 데이터를 가져오는 쿼리 -->
    <select id="getAllTravelInfo" resultType="com.example.domain.TravelInfoVO">
        SELECT * FROM TRAVEL_INFO
    </select>

    <!-- 특정 info_id를 기준으로 데이터를 가져오는 쿼리 -->
    <select id="getTravelInfoById" resultType="com.example.domain.TravelInfoVO">
        SELECT * FROM TRAVEL_INFO
        WHERE INFO_ID = #{info_id}
    </select>
    
    <!-- plan_id로 plan 가져옴 -->
	<select id="selectPlan" resultType="hashmap">
		SELECT 
            tp.plan_day,
            tp.plan_seq,
            a.attr_name
        FROM 
            travel_plan tp
        JOIN 
            attr a ON tp.attr_id = a.attr_id
        WHERE 
            tp.info_id = #{infoId}
        ORDER BY 
            tp.plan_day, tp.plan_seq
	</select>

	<select id="calDates" resultType="java.lang.String">
	<![CDATA[  
		WITH date_range AS (
		    SELECT
		        TO_DATE('2024-08-21', 'YYYY-MM-DD') + LEVEL - 1 AS day
		    FROM
		        dual
		    CONNECT BY
		        LEVEL <= (TO_DATE('2024-09-02', 'YYYY-MM-DD') - TO_DATE('2024-08-21', 'YYYY-MM-DD') + 1)
		)
		SELECT TO_CHAR(day, 'YYYY-MM-DD') AS formatted_day
		FROM date_range
	]]>
	</select>
	
</mapper>