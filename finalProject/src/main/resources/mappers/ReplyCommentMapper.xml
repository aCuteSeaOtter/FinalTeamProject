<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ReplyCommentDAO">

	<insert id="insertReplyComment" parameterType="ReplyCommentVO">
        INSERT INTO reply2comment (RNO, replyComment, replyCommenter, replyCommentdate)
        VALUES (#{RNO}, #{replyComment}, #{replyCommenter}, sysdate())
    </insert>

    <select id="selectAllReplyComment" parameterType="int" resultType="ReplyCommentVO">
        SELECT *
        FROM reply2comment
        WHERE RNO = #{rno}
    </select>
    
    <delete id="deleteReplyComment" parameterType='int'>
    	DELETE FROM reply2comment WHERE rcno=#{rcno}
    </delete>
</mapper>