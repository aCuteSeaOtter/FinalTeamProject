<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.dao.AttrDAO">

	<!-- popup에 모든 attr 리스트 가져오기 (attr_id를 받았다면 맞는 값 가져오기) -->
    <select id="selectAttrList" resultType="com.example.domain.AttrVO">
        SELECT *
        FROM ATTR
        <where>
	        <!-- <if test="attr_id != null">
	            AND attr_id = #{attr_id}
	        </if> -->
	        <if test="local != null">
	            AND info_addr LIKE '%' || #{local} || '%'
	        </if>
	    </where>
    </select>
    
    <!-- attr_id로 attr 리스트 가져오기 -->
    <select id="selectAttrListById" resultType="hashmap">
        SELECT 	a.attr_id attr_id, a.attr_name attr_name, a.attr_local attr_local, a.attr_lat attr_lat, a.attr_lon attr_lon, tp.plan_seq plan_seq
        FROM 	attr a
        JOIN 	travel_plan tp	ON	a.attr_id = tp.attr_id
        WHERE 	tp.attr_id = #{id} AND tp.info_id = #{info_id}
    </select>
    
    <!-- myPage attr 리스트 받아오기 -->
    <select id="editAttrList" resultType="com.example.domain.AttrVO">
        SELECT *
        FROM ATTR
        <where>
	        <!-- <if test="attr_id != null">
	            AND attr_id = #{attr_id}
	        </if> -->
	        <if test="trip_place != null">
	            AND info_addr LIKE '%' || #{trip_place} || '%'
	        </if>
	    </where>
    </select>
    
    <!-- 검색어를 이용한 명소 검색 -->
    <select id="searchAttrByKeyword" resultType="com.example.domain.AttrVO">
	    SELECT * FROM attr
	    WHERE attr_name LIKE CONCAT(CONCAT('%', #{keyword}), '%')
	       OR attr_local LIKE CONCAT(CONCAT('%', #{keyword}), '%')
	</select>
	
	<!-- 명소 정보 가져오기 -->
    <select id="selectAttrInfo" resultType="com.example.domain.AttrVO">
	    SELECT * 
	    FROM attr
	    WHERE attr_id = #{attr_id}
	</select>
    
</mapper>